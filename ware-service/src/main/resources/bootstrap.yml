spring:
  application:
    name: ware-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yml
  profiles:
    active: dev
rocketmq:
  producer:
    group: test-group


